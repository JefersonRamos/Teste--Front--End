To generate the endpoint key, go to https://crudcrud.com and copy and paste the token into services / auth.js

Replace the value ENDPOINT TOKEN for the redeemed key

auth.js
  const KEY = "END POINT TOKEN CRUD CRUD";
